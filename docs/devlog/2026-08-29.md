## 08:23 〜 v1 ディレクトリ構成の骨組みと migrate.ts の実装

### やったこと
- SPEC.md のディレクトリ構成に沿って `package.json` / `tsconfig.json` / `migrations/` / `src/server/` / `tests/server/` を新規作成
- `migrations/001_init.sql` に SPEC.md のデータモデル(`pages`, `page_revisions`, `links`, `pages_fts`(FTS5 trigram), `schema_version`)をそのまま定義
- `src/server/migrate.ts` を実装。`schema_version` テーブルの有無 → 適用済みバージョン集合を求め、未適用の `migrations/*.sql` を番号順・1ファイル1トランザクションで適用し、成功ごとに `schema_version` へ番号を記録する
- `tests/server/migrate.test.ts` で以下を確認
  - 空 DB に `001_init.sql` を適用すると SPEC 通りの表一式ができる
  - 一時ディレクトリに複数マイグレーションを用意して「空 DB からの全適用」「途中(001 だけ適用済み)から再開して 002 が追加された後の再開」「同じ migrate() を複数回呼んでも二重適用にならない」を検証
  - FTS5 の shadow table(`pages_fts_*`)や `sqlite_sequence` はマイグレーションが直接定義した表ではないため、テーブル一覧の比較から除外
- `bun install` → `bun test` / `bun run typecheck` が通ることを確認
- 実装前に ADR-0001(Bun/bun:sqlite/React 選定)・ADR-0002(番号付き SQL + schema_version によるマイグレーション方式)を Accepted で起票(SPEC.md にすでにある決定の遡及ドキュメント化)

### 詰まった点・気づき
- `schema_version` テーブル自体を `001_init.sql` の中で作る設計なので、`migrate.ts` は「テーブルが無ければ未適用 0 件」という形でブートストラップする必要があった。専用の管理テーブルを別に持たない分、この分岐が唯一の複雑ポイント
- FTS5(`tokenize='trigram'`)は内部的に `_data` / `_idx` / `_docsize` / `_config` の shadow table を作る。マイグレーションのテスト側でテーブル一覧を突き合わせるときはこれらを除外しないと壊れる
- `server.ts` / `db.ts` / `pages.ts` などディレクトリ構成上の他のファイルは、今回のタスク(001_init.sql と migrate.ts)の範囲外として未着手のまま残した

### 関連ADR / コミット
- ADR-0001, ADR-0002

## 10:14 〜 db.ts の実装

### やったこと
- `src/server/db.ts` を実装。`createDb(path)` が親ディレクトリを作成 → `new Database(path)` → `migrate(db)` を呼んで返すだけの薄いラッパー(デフォルトパスは `data/pages.db`)
- `tests/server/db.test.ts` で `:memory:` 指定時に migrate が実行されること、ファイルパス指定時に存在しない親ディレクトリを作ってから DB ファイルができることを確認
- `.gitignore` に `data/` を追加。`db.ts` のデフォルトパスが `data/pages.db` になったことで、CLAUDE.md の「`data/` 配下を git に入れない」ルールが実際に効くようにする抜け漏れだったので合わせて直した

### 詰まった点・気づき
- `createDb()` をモジュールトップレベルで即実行するシングルトンにすると、テストで `db.ts` を import しただけで実 DB ファイル(`data/pages.db`)ができてしまう。ファクトリ関数のままにして `server.ts`(未実装)側で一度だけ呼ぶ形にし、副作用を持たせないようにした

### 関連ADR / コミット
- なし(migrate.ts の薄いラッパーで新規の設計判断は無し)

## 10:22 〜 src/shared/syntax.ts の実装

### やったこと
- `src/shared/syntax.ts` に SPEC.md の記法(v1)表を一通り実装
  - `parseLine(line): Node[]`: `[タイトル]` / `#タグ` → `link`、`[* 強調]` → `bold`(`*` の数を level に)、`[https://...]` → `url`、`[/files/<hash>.png]` `[https://...png]` → `image`、`code:lang` 単独行 → `codeBlockStart`、それ以外は `text`
  - `indentOf(line): number`: 行頭の空白/タブの文字数(箇条書きのネスト深さ用)
- `tests/shared/syntax.test.ts` に記法ごとのパース結果テストを追加(空行、複数記法の混在、`[*not-bold]` のような非マッチケース、code:lang の誤検出防止など)
- `bun test` / `bun run typecheck` が通ることを確認

### 詰まった点・気づき
- CLAUDE.md の「1行を受け取ってノード配列を返す」という制約と、SPEC.md の「code:lang の次行以降、インデントされた行がコードブロック」という記法は素直には両立しない(コードブロックの範囲は複数行にまたがる状態を要る)。今回は `parseLine` は `code:lang` という開始マーカー行の検出だけを担当することにし、「次の行以降をコード行として扱うかどうか」の状態管理は呼び出し側(未実装の `links.ts` / `LineRenderer`)に委ねる設計にした
- `tsconfig.json` の `noUncheckedIndexedAccess` により、正規表現マッチ結果の添字アクセス(`match[1]` など)が `string | undefined` 扱いになりコンパイルエラーになった。マッチ済みで捕捉が保証されている箇所なので `!` で明示した

### 関連ADR / コミット
- なし(SPEC.md に記法表としてすでにある仕様の実装で、新規の設計判断は無し)

## 11:43 〜 src/shared/types.ts の実装

### やったこと
- `src/shared/types.ts` に `Page` / `Revision` 型を追加。SPEC.md の `pages` / `page_revisions` テーブル定義にフィールド名をそのまま合わせた(`deleted_at`, `page_id`, `saved_at` など snake_case のまま)
- `lines` は DB 上は `TEXT(JSON)` だが、型としては `JSON.parse` 済みの `string[]` として定義(DB との変換は今後 `pages.ts` が担う)
- 純粋な型定義のみで実行時ロジックが無いため専用のテストは追加せず、`bun test` / `bun run typecheck` が通ることのみ確認

### 詰まった点・気づき
- フィールド名を DB カラム名(snake_case)に合わせるか、TS の慣習である camelCase にするか迷った。`migrate.ts`/`db.ts` のテストがすでに `{ v: number }` のように SQL の結果をそのままの列名で受け取っている前例に合わせ、変換レイヤーを増やさない方針で snake_case のまま採用した

### 関連ADR / コミット
- なし(型定義のみで新規の設計判断は無し)

## 11:47 〜 src/server/links.ts の実装

### やったこと
- `src/server/links.ts` を実装
  - `extractLinkTitles(lines)`: `parseLine` の結果から `link` ノードのタイトルを初出順・重複なしで集める。`code:lang` 行より深くインデントされた行はコードブロックの中身としてスキップ(syntax.ts が先送りした行跨ぎの状態管理はここが担当)
  - `rebuildLinks(db, pageId, lines)`: `links` を DELETE → INSERT で作り直す(CLAUDE.md の導出データ方針どおり)。トランザクションで囲む
  - `backlinks(db, pageId)`: `links.to_title` = 自ページのタイトル、`deleted_at IS NULL`、自己リンク除外、更新日時降順
  - `twoHop(db, pageId)`: リンク先ページ(削除済み除外)がさらにリンクしている実在ページを DISTINCT で返す。自分自身は除外
- 返り値は `lines` を含まない軽量な `PageRef {id, title}` にした(バックリンク一覧の表示にはタイトルがあれば足りる)
- テスト: 抽出(タグ・重複・コードブロック除外・ブロック終端)、再生成の置き換え・他ページ非干渉、A→B→C 構造でのバックリンク / 2ホップ、削除済み・自己参照の除外

### 詰まった点・気づき
- 2ホップの結果から「直接リンク先」を除くかは SPEC に記載がない。今回は自分自身のみ除外し、直接リンク先が2ホップにも現れるケースは許容した(表示で気になったら後で絞る)
- リンク抽出はタイトル行(1行目)も対象に含めた。タイトルに `[...]` を書くことは実用上ほぼ無く、除外の分岐を増やすより単純さを取った

### 関連ADR / コミット
- なし(SPEC.md 記載の仕様の実装)

## 11:52 〜 src/server/pages.ts の実装(CRUD + リビジョン + 論理削除)

### やったこと
- 実装前に ADR-0003(10分ルールによるリビジョンの積み方)を Accepted で起票
- `ulid` を依存に追加(CLAUDE.md の許可リスト内。ページ ID 用)
- `src/server/pages.ts` を実装
  - `listPages` / `getPage`: `deleted_at IS NULL` で絞る。一覧は更新日時降順
  - `getByTitle`: 未存在なら ULID を振って空ページ(`lines = [title]`)を作成。削除済み同名ページがある場合は、内容をリビジョンに退避してから同じ行を空ページとして蘇生(title UNIQUE + 物理削除禁止のため新規 INSERT はできない)
  - `save`: 毎回 pages・links・pages_fts を更新し、リビジョンは「内容が直前と異なる かつ(初回 / 10分経過 / 離脱時)」のときだけ積む。判定はすべて save() 内(CLAUDE.md)
  - 改名時は `links.to_title` で旧タイトルへのリンク元を探し、本文の `[旧title]` を `[新title]` に置換して links / fts を追随させる
  - `deletePage`: `deleted_at` を立てるだけ
  - `listRevisions`(json_array_length で行数算出・新しい順)/ `getRevision` / `restore`(現在の状態を積む → 上書き)
- `pages_fts` の再生成も pages.ts に置いた(body は `lines.join("\n")`)
- テスト18件追加: 作成・蘇生・リビジョン判定4パターン・改名伝播・論理削除・restore の順序など

### 詰まった点・気づき
- 削除済みページと同名の新規作成は SPEC に記載がない穴だった。title UNIQUE と物理削除禁止が両立する以上 INSERT はできないので、「旧内容をリビジョンに退避 → 同じ行を空にして蘇生」とした。削除前の本文は履歴から辿れる
- 改名伝播で他ページの `updated` は変えないことにした(改名の巻き添えで一覧の並び順が動くのは不自然)。また SPEC どおり `[旧title]` のみ置換し、`#旧title` 形式は置換しない(その分 links に旧タイトルへのリンクが残るが、リンク解決時に未存在ページ扱いになるだけ)
- 時刻は全関数で `now` 引数として注入可能にした。10分ルールのテストが実時間に依存せず書ける

### 関連ADR / コミット
- ADR-0003

## 14:04 〜 src/server/search.ts の実装

### やったこと
- `src/server/search.ts` を実装
  - クエリのコードポイント数(`[...q].length`)が3以上なら FTS5 `MATCH`、それ未満は `LIKE` フォールバック(CLAUDE.md の分岐そのまま)
  - FTS5 に渡すクエリは `"..."` で囲み、内部の `"` を `""` にエスケープ。ユーザ入力が MATCH 構文として解釈されない
  - LIKE 側は `%` `_` `\` を `ESCAPE '\'` でエスケープし、ワイルドカード注入を防ぐ
  - どちらの経路も `deleted_at IS NULL` で絞る。並びは FTS が rank、LIKE は更新日時降順
- LIKE の本文マッチは `pages.lines`(JSON文字列)ではなく `pages_fts.body`(素のテキスト)に当てた。JSON のエスケープ(`\"` など)が部分一致に混ざるのを避けるため
- テスト10件: 3文字境界、サロゲートペア(𩸽𩸽 = UTF-16 では length 4)、日本語 FTS/LIKE、`"` を含むクエリ、`%`/`_` の非ワイルドカード化、削除済み除外、タイトル・本文両ヒット

### 詰まった点・気づき
- 2文字クエリで `"` そのものを本文検索するケースは、`pages_fts.body` に当てる方式にしたことで JSON エスケープ問題ごと消えた。`lines` に LIKE していたら `\"` に阻まれて当たらなかった
- FTS5 の仮想テーブルは JOIN 時にエイリアスを付けると `MATCH` の左辺に使えるか処理系依存で怪しいので、`pages_fts` をテーブル名のまま参照した

### 関連ADR / コミット
- なし(trigram + LIKE フォールバック方式は ADR-0001/SPEC.md 記載の決定の実装)

## src/server/upload.ts の実装(画像の sha256 保存)

### やったこと
- `src/server/upload.ts` を実装
  - `saveImage(bytes, contentType, dir)`: 内容の sha256 をファイル名にして `data/images/<hash>.<ext>` に保存し、`/files/<hash>.<ext>` の相対 URL を返す(SPEC.md。ホスト名は本文に入れない)
  - 拡張子は Content-Type から決定(png / jpg / gif / webp / svg)。未知のタイプは throw して routes.ts 側でエラー応答にする想定
  - 同一内容は同名ファイルに収束するので、既存チェックして二重書き込みしない
  - `resolveImagePath(name, dir)`: `GET /files/:name` 配信用。`[0-9a-f]{64}\.既知拡張子` に一致する名前だけパスを返し、パストラバーサルを形式で締め出す
- テスト: 既知の sha256 値("hello")での保存・URL・内容一致、重複保存の収束、jpeg→.jpg、未対応タイプの reject、トラバーサル・不正名の null

### 詰まった点・気づき
- 配信側の安全性はパス正規化ではなく「名前の形式チェック」に寄せた。ハッシュ命名のおかげで正当なファイル名の全集合が正規表現1本で書けるので、`..` や `/` の混入をケース列挙で防ぐより堅い
- ハッシュ計算は `Bun.CryptoHasher`(server 専用モジュールなので Bun API 依存で問題ない)

### 関連ADR / コミット
- なし(sha256 命名・相対 URL は SPEC.md 記載の決定の実装)

## src/server/routes.ts の実装(API 結線)

### やったこと
- `src/server/routes.ts` を実装。`createRoutes(db, {imagesDir})` が SPEC.md の API 一覧をそのまま `Bun.serve` の routes 形式で返す。server.ts はこれをスプレッドするだけの想定
- 例外→HTTP 変換はこの層に集約(CLAUDE.md)。メッセージで分類: `not found`→404、`UNIQUE constraint failed`→409、`invalid...`/`unsupported image type`→400、それ以外→500
- PUT の body は手書きバリデーション(title 非空文字列・lines 文字列配列)。`leaving: true` を任意で受け取り save() の離脱時フラグに渡す
- アップロードは multipart ではなく「body に Blob 直接 + Content-Type ヘッダ」方式にした(クライアントは自作なので最小の方が楽。curl でも `--data-binary` で試せる)
- テストはモックせず実サーバで実施: `Bun.serve({port: 0, routes})` + `:memory:` DB + 一時 imagesDir を毎テスト起動し、fetch で13ケース(バックリンク/2ホップ入りの GET、400/404/409、履歴一巡、画像の往復など)

### 詰まった点・気づき
- Bun の routes はパスパラメータを URL デコード済みで渡してくれる(事前に小さなスクリプトで確認)。`by-title/日本語` がそのまま通るので自前デコード不要
- `Server` 型を bun からの import で書くと `Server<WebSocketData>` のジェネリクス必須で typecheck が落ちた。`ReturnType<typeof Bun.serve>` で受けるのが楽
- `/api/pages/by-title/:title` と `/api/pages/:id` はセグメント数が違うので Bun のルーティング上衝突しない

### 関連ADR / コミット
- なし(SPEC.md の API 一覧の実装)

## server.ts と最小クライアント(一覧 + ページ作成)

### やったこと
- `server.ts` を実装。`createDb()`(起動時マイグレーション込み)→ `Bun.serve` に `createRoutes(db)` と HTML import(`"/": index`)を並べるだけの薄いエントリ
- `src/client/` を新設: `index.html` / `styles.css` / `api.ts`(fetch ラッパ)/ `main.tsx`
- クライアントは v1 の最小形として「更新日時降順のカードグリッド(タイトル + 先頭数行)」と「タイトル入力 → by-title で作成」だけ。PageView / Editor は次回
- `react` / `react-dom`(+ 型)を依存に追加(CLAUDE.md 許可リスト内)。`tsconfig.json` の lib に DOM を追加
- 実サーバを起動して E2E 確認: `GET /` で HTML とバンドル済み JS/CSS が配信される、`by-title` で日本語タイトルのページが ULID 付きで作られる、`data/pages.db` が自動生成される

### 詰まった点・気づき
- Bun の HTML import は `<script src="./main.tsx">` と `<link href="./styles.css">` を起動時にバンドルして `/_bun/...` 配下で配信してくれる。ビルド設定ゼロで React + TSX が動くのは楽
- tsconfig の lib に DOM を足すと `src/shared/` からも DOM 型が見えてしまう。「shared に DOM 依存を持ち込まない」はコンパイラでは強制されなくなるので、レビューで守る運用(分割 tsconfig は v1 ではやり過ぎと判断)

### 関連ADR / コミット
- なし(Bun.serve + HTML import / Vite 不使用は ADR-0001 の決定)

## PageView + Editor(自動保存・離脱時保存)とハッシュルーティング

### やったこと
- `src/client/pages/` と `src/client/components/` を新設し、main.tsx をルータだけに縮小
  - ルーティングはハッシュのみ(`#/` = 一覧、`#/p/<id>` = ページ)。ルータライブラリは入れない
  - `ListPage`: カードを `#/p/<id>` へのリンクにし、作成後はそのページへ遷移
  - `PageView`: 取得 → Editor + バックリンク / 2ホップの一覧(`RefList`)。id 変更時は `key` で Editor を作り直す
  - `Editor`: 本文全体を1つの textarea で編集。500ms デバウンスで自動保存、アンマウントと beforeunload で `leaving: true` 保存。直前保存と同一内容ならスキップ、タイトル行が空なら保存しない
- `PageRef` を `src/server/links.ts` から `src/shared/types.ts` へ移動。クライアントが server モジュールを import すると `bun:sqlite` がバンドルに混ざるため、API ペイロードの型は shared に置く
- `api.ts` に `getPage` / `savePage` を追加。`savePage` は `keepalive: true` で fetch(beforeunload からの送信が打ち切られないように)
- Playwright(scratchpad に playwright-core、プロジェクト依存には入れない)+ 同梱 Chromium で実ブラウザ E2E: 作成 → 遷移 → 自動保存がリロード後も残る → リンク先ページにバックリンクが表示される → カードクリック遷移、まで確認

### 詰まった点・気づき
- 離脱時保存は「SPA 内の画面遷移(アンマウント)」と「タブを閉じる/リロード(beforeunload)」の2経路がある。どちらも Editor 内の同じ `doSave(true)` に寄せ、textarea の最新値は ref 経由で読むことで stale closure を回避した
- fetch の keepalive は body 64KB 制限がある。個人ノートの1ページなら実用上超えない想定で許容
- 保存後の `PageView` は backlinks / twoHop を再取得しない(自分の編集で自分へのバックリンクは変わらない)。他ページの表示は次回開いたときに反映される

### 関連ADR / コミット
- なし(textarea 編集・デバウンス 500ms・離脱時保存は SPEC.md 記載の仕様)

## LineRenderer(記法の描画)と表示/編集トグル

### やったこと
- `src/shared/syntax.ts` に `groupLines(lines): LineBlock[]` を追加。行を跨ぐ唯一の構造であるコードブロックをブロックにまとめ、通常行はインデントを剥がして `parseLine` したノード列にする純粋関数(テスト5件)
- `src/client/components/LineRenderer.tsx`: ブロックを描画。`[リンク]`/`#タグ` → `#/t/<title>` へのアンカー、外部リンクは新規タブ、画像は `<img>`、強調は `*` の数で文字サイズ、コードは `<pre><code>`、インデントは字下げ+ビュレット
- `main.tsx` に `#/t/<title>` ルート(`TitleRedirect`)を追加。by-title で解決(未存在なら自動作成)して `location.replace` で `#/p/<id>` に置き換える(履歴に解決画面を残さない)
- `PageView` に表示/編集トグルを追加。本文ありのページは表示モードで開き、作りたて(タイトル行のみ)は編集モードで開く。編集→表示の切り替えは Editor のアンマウント = 離脱時保存として機能する
- Playwright E2E: 記法一式の描画(h1・コードブロック・bold-3・ビュレット・外部リンク)、`[未存在ページ]` クリック→自動作成→編集モード、トグルでの保存反映、バックリンク表示を確認

### 詰まった点・気づき
- syntax.ts の `Node` 型はクライアントでは DOM の `Node` と名前が被る。import 時に `as SyntaxNode` で別名にして回避
- コードブロックの中身はマーカーより1段深いインデントを剥がして表示することにした(`code.indent + 1` 文字を slice)。タブ/スペース混在は「1文字=1段」の indentOf の規約に従う
- `links.ts` の `extractLinkTitles` にも同じコードブロックスキップのロジックが残っている。`groupLines` を使う形に寄せられるが、動いてテストもあるので今回は触らず(次の整理候補)

### 関連ADR / コミット
- なし(記法の描画仕様は SPEC.md の記法表どおり)

## links.ts のコードブロックスキップを groupLines に集約

### やったこと
- `extractLinkTitles` が自前で持っていたコードブロックの行跨ぎ状態管理(LineRenderer 用の `groupLines` と重複)を削除し、`groupLines` の line ブロックからリンクノードを集めるだけの実装に置き換えた
- 挙動は不変。既存テスト(コードブロック除外・ブロック終端・重複排除・初出順)が変更なしで全通過することで確認

### 詰まった点・気づき
- 「コードブロック内はリンクにならない」というルールの実装がサーバ(リンク抽出)とクライアント(描画)で1箇所になった。今後この規則を変えるときは groupLines だけ直せばよい

### 関連ADR / コミット
- なし(挙動を変えないリファクタリング)
